[project]
name = "alignment-adventure"
version = "0.1.0"
description = "A retro AI safety platformer built for a game jam."
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "pygame>=2.6.1",
]

[tool.uv]
package = false
